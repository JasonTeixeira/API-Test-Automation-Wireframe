# API Test Framework Architecture

## 📋 Overview

This document provides detailed architectural documentation for the API Test Automation Framework, including system design, component interactions, and visual representations.

---

## 🏗️ High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                     CI/CD Pipeline (GitHub Actions)              │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐          │
│  │  Code    │ │  Smoke   │ │   API    │ │ Coverage │          │
│  │ Quality  │→│  Tests   │→│ Tests(5) │→│ Analysis │          │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘          │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                      Test Execution Layer                        │
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │   Pytest     │  │   Fixtures   │  │   Markers    │         │
│  │   Runner     │→ │   (20+)      │→ │   (9 types)  │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
│                              ↓                                   │
│  ┌─────────────────────────────────────────────────────┐       │
│  │              Test Suites (125+ tests)                │       │
│  │  Users(35) | Resources(28) | Auth(28) | More...     │       │
│  └─────────────────────────────────────────────────────┘       │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                      Client Abstraction Layer                    │
│                                                                   │
│  ┌──────────────────────────────────────────────────────┐       │
│  │                    BaseClient                         │       │
│  │  • Retry Logic  • Logging  • Session Mgmt           │       │
│  └──────────────────────────────────────────────────────┘       │
│                 ↓              ↓              ↓                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │   Users      │  │  Resources   │  │     Auth     │         │
│  │   Client     │  │   Client     │  │   Client     │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                     HTTP Transport Layer                         │
│                                                                   │
│  ┌──────────────────────────────────────────────────────┐       │
│  │              Python Requests Library                  │       │
│  │         • Connection Pooling                          │       │
│  │         • Session Management                          │       │
│  │         • SSL/TLS Support                            │       │
│  └──────────────────────────────────────────────────────┘       │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                        ReqRes API                                │
│                    (https://reqres.in/api)                       │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Request Flow Architecture

```
Test Execution Flow
═══════════════════

┌──────────┐
│   Test   │  1. Test invokes client method
│ Function │     with test data
└─────┬────┘
      │
      ↓
┌─────────────────┐
│ Specific Client │  2. Client prepares request
│ (Users/Auth)    │     • Builds URL
└─────┬───────────┘     • Adds headers
      │                  • Validates inputs
      ↓
┌─────────────────┐
│   BaseClient    │  3. BaseClient handles
│                 │     • Retry logic
│   • request()   │     • Logging (sanitized)
│   • retry       │     • Session mgmt
│   • logging     │     • Error handling
└─────┬───────────┘
      │
      ↓
┌─────────────────┐
│    Requests     │  4. HTTP library makes
│    Library      │     actual API call
└─────┬───────────┘
      │
      ↓
┌─────────────────┐
│   ReqRes API    │  5. API processes and
│                 │     returns response
└─────┬───────────┘
      │
      ↓
┌─────────────────┐
│  APIResponse    │  6. Wrap response with
│    Wrapper      │     • Status code
│                 │     • JSON data
│  • is_success() │     • Elapsed time
│  • json         │     • Helper methods
│  • elapsed_time │
└─────┬───────────┘
      │
      ↓
┌─────────────────┐
│ Test Assertions │  7. Test validates
│                 │     • Status codes
│  assert         │     • Response data
│  validate       │     • Performance
│  verify         │     • Schema
└─────────────────┘
```

---

## 🎯 Component Architecture

### 1. Client Layer

```
Client Architecture
══════════════════

                    ┌────────────────────┐
                    │    BaseClient      │
                    │  (Abstract Layer)  │
                    ├────────────────────┤
                    │ • request()        │
                    │ • get()            │
                    │ • post()           │
                    │ • put/patch/delete │
                    │ • retry_logic()    │
                    │ • session_mgmt()   │
                    │ • logging()        │
                    └──────┬─────────────┘
                           │
                           │ Inheritance
           ┌───────────────┼───────────────┐
           │               │               │
           ↓               ↓               ↓
    ┌──────────┐   ┌──────────┐   ┌──────────┐
    │  Users   │   │Resources │   │   Auth   │
    │  Client  │   │  Client  │   │  Client  │
    ├──────────┤   ├──────────┤   ├──────────┤
    │get_users │   │get_resource│  │login()   │
    │get_user  │   │create_res │   │register()│
    │create    │   │update_res │   │logout()  │
    │update    │   │delete_res │   │validate  │
    │delete    │   │           │   │          │
    └──────────┘   └──────────┘   └──────────┘
```

**Features:**
- **Retry Logic**: Exponential backoff with configurable attempts
- **Logging**: Request/response logging with sensitive data sanitization
- **Session Management**: Persistent connections with connection pooling
- **Error Handling**: Graceful error handling and recovery
- **Performance Tracking**: Response time measurement

---

### 2. Model Layer

```
Pydantic Model Hierarchy
════════════════════════

    ┌──────────────────┐
    │   BaseModel      │
    │   (Pydantic)     │
    └────────┬─────────┘
             │
      ┌──────┴──────┬──────────────┬──────────────┐
      │             │              │              │
      ↓             ↓              ↓              ↓
┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐
│   User   │  │ Resource │  │  Login   │  │  Error   │
│  Models  │  │  Models  │  │  Models  │  │  Models  │
├──────────┤  ├──────────┤  ├──────────┤  ├──────────┤
│• User    │  │• Resource│  │• LoginReq│  │• Error   │
│• UserList│  │• ResList │  │• LoginRes│  │  Response│
│• Created │  │• Created │  │• Register│  │          │
│• Updated │  │• Updated │  │• Token   │  │          │
└──────────┘  └──────────┘  └──────────┘  └──────────┘
      │             │              │              │
      └─────────────┴──────────────┴──────────────┘
                           │
                    Validation Layer
                           │
            ┌──────────────┼──────────────┐
            │              │              │
      Email Format   Custom Rules   Field Types
      Validators     Validators     Validators
```

**Validation Features:**
- Type checking at runtime
- Custom field validators
- Email format validation
- URL validation
- Required/optional fields
- Min/max constraints
- Pattern matching (regex)

---

### 3. Test Organization

```
Test Suite Architecture
══════════════════════

┌─────────────────────────────────────────────────────┐
│                  conftest.py                         │
│         (Fixtures & Configuration Hub)               │
│                                                      │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐   │
│  │  Client    │  │  Test      │  │  Helper    │   │
│  │  Fixtures  │  │   Data     │  │  Fixtures  │   │
│  └────────────┘  └────────────┘  └────────────┘   │
└────────┬──────────────────┬──────────────┬─────────┘
         │                  │              │
         └──────────┬───────┴──────┬───────┘
                    │              │
    ┌───────────────┴───┬──────────┴────────┬──────────┐
    │                   │                   │          │
    ↓                   ↓                   ↓          ↓
┌─────────┐      ┌──────────┐      ┌──────────┐  ┌─────────┐
│  Users  │      │Resources │      │   Auth   │  │Workflows│
│  Tests  │      │  Tests   │      │  Tests   │  │  Tests  │
│  (35)   │      │   (28)   │      │   (28)   │  │  (11)   │
├─────────┤      ├──────────┤      ├──────────┤  ├─────────┤
│• CRUD   │      │• CRUD    │      │• Login   │  │• E2E    │
│• Page   │      │• Data    │      │• Register│  │• Multi  │
│• Schema │      │• Schema  │      │• Security│  │• State  │
│• Perf   │      │• Valid   │      │• Tokens  │  │• Flow   │
└─────────┘      └──────────┘      └──────────┘  └─────────┘
```

---

## 📊 Data Flow Diagram

```
Complete Data Flow
═════════════════

[Test] → [Fixture] → [Client] → [BaseClient] → [HTTP] → [API]
   ↓         ↓          ↓            ↓           ↓        ↓
[Assert] ← [Model] ← [Wrapper] ← [Response] ← [JSON] ← [Data]


Detailed Breakdown:
══════════════════

1. TEST INITIATION
   ┌──────────────┐
   │ Test Function│
   └──────┬───────┘
          │ Uses fixtures
          ↓
   ┌──────────────┐
   │   Fixtures   │
   │• users_client│
   │• user_data   │
   │• auth_token  │
   └──────┬───────┘

2. DATA PREPARATION
          │
          ↓
   ┌──────────────┐
   │ Test Data    │
   │ Generator    │
   │• Faker data  │
   │• Valid data  │
   │• Invalid data│
   └──────┬───────┘

3. CLIENT INVOCATION
          │
          ↓
   ┌──────────────┐
   │ Specific     │
   │ Client       │
   │• users_client│
   │• auth_client │
   └──────┬───────┘

4. REQUEST PROCESSING
          │
          ↓
   ┌──────────────┐
   │  BaseClient  │
   │• Build URL   │
   │• Add headers │
   │• Log request │
   └──────┬───────┘

5. HTTP TRANSPORT
          │
          ↓
   ┌──────────────┐
   │   Requests   │
   │   Library    │
   │• HTTP call   │
   │• SSL/TLS     │
   └──────┬───────┘

6. API RESPONSE
          │
          ↓
   ┌──────────────┐
   │  ReqRes API  │
   │• Process req │
   │• Return JSON │
   └──────┬───────┘

7. RESPONSE WRAPPING
          │
          ↓
   ┌──────────────┐
   │ APIResponse  │
   │ Wrapper      │
   │• Parse JSON  │
   │• Track time  │
   └──────┬───────┘

8. VALIDATION
          │
          ↓
   ┌──────────────┐
   │   Pydantic   │
   │   Models     │
   │• Validate    │
   │• Type check  │
   └──────┬───────┘

9. ASSERTION
          │
          ↓
   ┌──────────────┐
   │ Test Assert  │
   │• Status OK   │
   │• Data valid  │
   │• Perf good   │
   └──────────────┘
```

---

## 🔐 Security Architecture

```
Security Testing Layer
═════════════════════

┌─────────────────────────────────────────────────┐
│           Security Test Suite                    │
│                                                  │
│  ┌──────────────┐  ┌──────────────┐           │
│  │ SQL Injection│  │     XSS      │           │
│  │   Testing    │  │   Testing    │           │
│  └──────┬───────┘  └──────┬───────┘           │
│         │                  │                    │
│         └──────────┬───────┘                    │
│                    │                            │
│         ┌──────────▼───────────┐               │
│         │  Payload Generator   │               │
│         │  • SQL payloads      │               │
│         │  • XSS payloads      │               │
│         │  • Unicode attacks   │               │
│         │  • Null bytes        │               │
│         └──────────┬───────────┘               │
│                    │                            │
│         ┌──────────▼───────────┐               │
│         │  Security Validators │               │
│         │  • No crashes        │               │
│         │  • Proper errors     │               │
│         │  • No data leaks     │               │
│         └──────────────────────┘               │
└─────────────────────────────────────────────────┘
```

---

## ⚡ Performance Architecture

```
Performance Tracking System
══════════════════════════

┌────────────────────────────────────────────┐
│         Performance Layer                   │
│                                             │
│  Request Start                              │
│       │                                     │
│       ↓                                     │
│  ┌─────────────┐                           │
│  │ time.time() │  Start Timer             │
│  └──────┬──────┘                           │
│         │                                   │
│         ↓                                   │
│  ┌─────────────┐                           │
│  │ HTTP Request│  Execute Request          │
│  └──────┬──────┘                           │
│         │                                   │
│         ↓                                   │
│  ┌─────────────┐                           │
│  │ time.time() │  End Timer               │
│  └──────┬──────┘                           │
│         │                                   │
│         ↓                                   │
│  ┌─────────────┐                           │
│  │  Calculate  │  Elapsed = End - Start   │
│  │  Elapsed    │                           │
│  └──────┬──────┘                           │
│         │                                   │
│         ↓                                   │
│  ┌─────────────────┐                       │
│  │  Performance    │                       │
│  │   Assertion     │                       │
│  │                 │                       │
│  │ assert elapsed  │                       │
│  │   < threshold   │                       │
│  └─────────────────┘                       │
│                                             │
│  Threshold: 1000ms (configurable)          │
└────────────────────────────────────────────┘
```

---

## 🐳 Docker Architecture

```
Docker Container Architecture
═════════════════════════════

┌──────────────────────────────────────────────────┐
│           docker-compose.yml                      │
│                                                   │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐│
│  │   test-    │  │   smoke-   │  │  parallel- ││
│  │  runner    │  │   tests    │  │   tests    ││
│  └─────┬──────┘  └─────┬──────┘  └─────┬──────┘│
└────────┼───────────────┼───────────────┼────────┘
         │               │               │
         └───────────────┼───────────────┘
                         │
            ┌────────────▼────────────┐
            │      Dockerfile         │
            │   (Multi-stage build)   │
            └────────────┬────────────┘
                         │
        ┌────────────────┼────────────────┐
        │                │                │
   ┌────▼─────┐   ┌─────▼──────┐   ┌────▼─────┐
   │  Stage 1 │   │  Stage 2   │   │  Stage 3 │
   │   Base   │→  │Dependencies│→  │   Test   │
   │  Python  │   │   Cached   │   │  Runner  │
   │  3.11    │   │    pip     │   │          │
   └──────────┘   └────────────┘   └──────────┘
```

---

## 📈 CI/CD Pipeline Architecture

```
GitHub Actions Workflow
══════════════════════

Trigger: Push/PR/Schedule
         │
         ↓
┌────────────────────┐
│   Code Quality     │  • Black formatter
│      Check         │  • isort imports
└────────┬───────────┘  • pylint linter
         │              • mypy types
         ↓
┌────────────────────┐
│   Smoke Tests      │  Fast feedback
│  (Critical Path)   │  ~30 seconds
└────────┬───────────┘
         │
         ├──────────────┬──────────────┬──────────┐
         ↓              ↓              ↓          ↓
    ┌────────┐    ┌─────────┐   ┌─────────┐  ┌────────┐
    │ Users  │    │Resources│   │  Auth   │  │Workflow│
    │ Tests  │    │  Tests  │   │  Tests  │  │ Tests  │
    └───┬────┘    └────┬────┘   └────┬────┘  └───┬────┘
        │              │             │           │
        └──────────────┴─────────────┴───────────┘
                       │
         ┌─────────────┼─────────────┐
         ↓             ↓             ↓
    ┌────────┐   ┌─────────┐   ┌─────────┐
    │Parallel│   │Coverage │   │ Allure  │
    │ Tests  │   │Analysis │   │ Report  │
    └────────┘   └─────────┘   └─────────┘
                       │
                       ↓
              ┌────────────────┐
              │   Notify       │
              │   Results      │
              └────────────────┘
```

---

## 🎯 Test Execution Strategy

```
Test Execution Pyramid
═════════════════════

         ┌──────────┐
         │  Smoke   │  ← 20 tests (Fast)
         │  Tests   │    ~30 seconds
         └─────┬────┘
               │
         ┌─────▼────────┐
         │  Integration  │  ← 40 tests (Medium)
         │    Tests      │    ~2 minutes
         └──────┬────────┘
                │
         ┌──────▼──────────┐
         │   Regression    │  ← 125 tests (Complete)
         │     Suite       │    ~5 minutes
         └─────────────────┘


Execution Modes:
═══════════════

1. Development: pytest tests/ -v
   └─ Run all tests locally

2. CI/CD Pull Request:
   ├─ Code quality checks
   ├─ Smoke tests (fast feedback)
   └─ Full test suite (parallel)

3. Nightly Build:
   ├─ Full regression suite
   ├─ Performance tests
   ├─ Security scans
   └─ Coverage analysis

4. On-Demand:
   └─ Manual trigger via GitHub Actions
```

---

## 📚 Key Design Patterns

### 1. **Client Abstraction Pattern**
```python
# Abstract away HTTP complexity
client = UsersClient()
response = client.get_users()  # Simple, clean API
```

### 2. **Fixture Pattern**
```python
# Reusable test components
@pytest.fixture
def users_client():
    client = UsersClient()
    yield client
    client.close()
```

### 3. **Model Validation Pattern**
```python
# Type-safe data validation
user = User(**response_data)  # Auto-validates
```

### 4. **Retry Pattern**
```python
# Automatic retry with backoff
@retry(stop=stop_after_attempt(3))
def request():
    # Handles transient failures
```

---

## 🔧 Configuration Management

```
Configuration Hierarchy
══════════════════════

┌─────────────────────┐
│   Environment       │  ← Highest priority
│   Variables         │
└──────────┬──────────┘
           │
┌──────────▼──────────┐
│    .env File        │  ← Environment-specific
└──────────┬──────────┘
           │
┌──────────▼──────────┐
│  settings.py        │  ← Default values
│  (Pydantic)         │
└─────────────────────┘

Config Flow:
1. Load .env file (if exists)
2. Override with environment variables
3. Apply Pydantic validation
4. Provide type-safe config object
```

---

## ✅ Quality Metrics

```
Quality Dashboard
════════════════

┌─────────────────────────────────────┐
│         Test Metrics                │
├─────────────────────────────────────┤
│ Total Tests:        125+            │
│ Pass Rate:          100%            │
│ Execution Time:     ~5 min          │
│ Code Coverage:      >80%            │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│         Code Metrics                │
├─────────────────────────────────────┤
│ Total LOC:          3,500+          │
│ Type Hints:         100%            │
│ Docstrings:         100%            │
│ Pylint Score:       9.5/10          │
└─────────────────────────────────────┘
```

---

This architecture demonstrates **enterprise-grade design** suitable for production environments and professional portfolios.
